// STUB — owned by Agent 3 (Weekly Tracker), plus src/lib/hooks/useWeek.ts.
// Route: / (see src/routes.tsx). Do not rename this file/path.

export function WeekPage() {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-2 text-center">
      <h1 className="text-lg font-semibold text-text-primary">Week</h1>
      <p className="max-w-sm text-sm text-text-secondary">
        Stub page — Agent 3 builds the weekly workout tracker here.
      </p>
    </div>
  );
}
